@model TokenQr

<h2>Token QR - @Model.IdToken</h2>

<p>Token: @Model.Token</p>

<!-- Muestra el QR generado dinámicamente -->
<img src="@Url.Action("Generar", "QR", new { token = Model.Token })" alt="QR para marcar asistencia" />
